Unit SyntaxEnum;

{$MODE Delphi}

Interface

Type TGleditLanguage = (glText, glJava, glPascal, glCpp, glHtml, glPhp,
                        glSql, glBasic, glXml, glPython, glJScript, glCss);

Implementation

End.
